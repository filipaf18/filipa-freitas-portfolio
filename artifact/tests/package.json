{
  "name": "nutriglp-tests",
  "private": true,
  "type": "module",
  "description": "Testes de browser da app NutriGLP (artifact). Corre `npm test` aqui dentro.",
  "scripts": {
    "test": "node run.mjs"
  },
  "devDependencies": {
    "pdfjs-dist": "3.11.174",
    "playwright-core": "^1.50.0"
  }
}
